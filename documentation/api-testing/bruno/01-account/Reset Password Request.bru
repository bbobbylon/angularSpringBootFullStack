meta {
  name: Reset Password Request
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/user/resetpassword/{{adminEmail}}
  body: none
  auth: none
}

tests {
  test("always 200 (user-enumeration protection)", function () {
    expect(res.getStatus()).to.equal(200);
  });
}

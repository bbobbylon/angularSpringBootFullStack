meta {
  name: List Customers
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/customer/list?page=0&size=10
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{adminAccessToken}}
}

params:query {
  page: 0
  size: 10
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}

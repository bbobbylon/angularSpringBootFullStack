meta {
  name: Get Service By Id
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/admin/services/get/{{serviceId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{adminAccessToken}}
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}

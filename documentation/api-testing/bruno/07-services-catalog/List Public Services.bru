meta {
  name: List Public Services
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/services/public
  body: none
  auth: none
}

tests {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}

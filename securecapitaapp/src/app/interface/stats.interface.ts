//TODO make the stats more detailed to allow for more interesting visualizations, e.g., total billed by month, average invoice amount, etc along with graphs
export interface StatsInterface {
  totalCustomers: number;
  totalInvoices: number;
  totalBilled: number;
}
